import { createTheme } from "@mui/material";

export default createTheme({
  palette: {
    background: {
      default: "#D6D9E8",
    },
  },
  shape: {
    borderRadius: 4,
  },
  typography: {
    button: {
      fontWeight: 700,
      textTransform: "unset",
    },
  },
});
